[package]
name = "utils"
version = "0.1.0"
edition = "2024"

[dependencies]
log = { version = "0.4", features = ["std"] }
parking_lot = "0.12"

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"
