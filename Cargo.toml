[package]
name = "utils"
version = "0.1.0"
edition = "2024"

[dependencies]
parking_lot = "0.12"

[features]
default = ["bitset", "channel", "future", "io", "log", "meta", "path", "sync"]

bitset = []
channel = []
future = []
io = []
log = []
meta = []
path = []
sync = []
